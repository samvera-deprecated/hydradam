        <tr>
          <th>Contributor</th>
          <td>
            <% @generic_file.contributor.each do |contributor| %>
              <%= link_to_field('contributor', Array(contributor.name).first) %><br />
            <% end %>
          </td>
        </tr>

